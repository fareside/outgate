{
  "name": "outgate",
  "version": "0.1.1",
  "description": "Run a command through a running Outgate proxy.",
  "type": "module",
  "bin": {
    "outgate": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "outgate",
    "proxy",
    "egress",
    "membrane"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/fareside/outgate.git",
    "directory": "javascript/outgate"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
